# razor

Blazing fast file explorer

## Search box

By default the search box uses multi-threading to search for both file contents as well as filenames. It uses a map to ensure
multiple results of the same file are not shown.
